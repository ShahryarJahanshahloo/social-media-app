import React from 'react';
import {Link} from "react-router-dom";

const Sidebar = (props) => {
    return (
        <div className="sidebar">
            <Link to="/"></Link>
        </div>
    )
};

export default Sidebar;